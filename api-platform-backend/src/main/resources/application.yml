server:
    port: 8101

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/api_open_platform?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: AccyCx
    password: ycy0050307

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


api:
  gateway:
    host: http://localhost:8090/api # API 网关地址
dubbo:
  application:
    name: api-platform-backend #服务名
  protocol:
    name: dubbo
    port: -1 #随机可用端口
  registry:
    address: nacos://localhost:8848 #注册到Nacos